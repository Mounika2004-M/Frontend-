// // Type casting
// //  ============
                        // var city = "hyderabad"
                        // // console.log('welcome to ${city}')//welcome to ${city}
                        // // console.log("welcome to ${city}");//welcome to ${city}
                        // console.log(`welcome to ${city}`); //welcome to hyderabad
                        // console.log(`welcome to ${"Chennai"}`);
                        // a=10
                        // b=20
                        // console.log(`welcome to ${a+b}`);
                        // console.log(`welcome to ${100+200}`);

// // Type casting (also called type conversion)
// //  means converting one data type to another, such as turning a string into a number, 
// // or a number into a string.

// // Two Types of Type Casting:
// // 1. Implicit Type Casting (Automatic by JavaScript)

// // JavaScript automatically converts types when needed.

// // Example:
                            // let result = "5" * 2;
                            // console.log(result);  // Output: 10

// "5" (a string) is automatically converted to a number.
// So "5" * 2 becomes 5 * 2 = 10.


// 2. Explicit Type Casting (Manual by developer)
// You manually convert from one type to another using functions like:

// Number()

// String()

// Boolean()

// parseInt(), parseFloat()

// 🔹 Examples:

                            // let str = "123";
                            // let num = Number(str);  // Now num is 123 (number)

                            // let n = 100;
                            // let s = String(n);  // Now s is "100" (string)

                            // let bool = Boolean(1);  // true



// Exampels
                            // var a =10
                            // typeof (a)//==> number
                            // String(a) //==> '10'
                            // Boolean(a) //==>true
 
                            // var b = 'hi';
                            // Number(b);  // ==> NaN

                            // var b = '100';
                             // Number(b) //==> 1000

                            var s = '100'
                            Number(s)
                            var z = ++s
                            console.log(z)
                            console.log(5+ +b);
                            // console.log(5++b);  //(wrong)
                            console.log(5+ ++b)



                            

